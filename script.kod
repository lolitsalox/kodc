/*
fib(n) {
    if n < 2 {
        return 1
    }
    return fib(n - 1) + fib(n - 2)
}
*/

f(x) {
    if x < 2 {
        return 1
    }
    return x + 1
}

i = 0
while i < 15 {
    print(f(i))
    i = i + 1
}